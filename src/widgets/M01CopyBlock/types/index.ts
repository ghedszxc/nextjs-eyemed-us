import { TWidgetImages, ThemeColor } from '@/types/theme'

export type TM01CopyBlock = {
  images?: TWidgetImages
  theme?: ThemeColor
  bannerTitle?: string
  bannerText?: string
  showTopBanner?: boolean
  topTitle?: string
  topText?: string
  isShort?: boolean
}
