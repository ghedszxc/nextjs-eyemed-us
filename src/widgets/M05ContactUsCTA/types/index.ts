import { ThemeColor } from '@/types/theme'
import { ICta } from '@/models/ICta'

export interface IM05ContactUsCTA {
  images?: any
  icon?: string
  title?: string
  subtitle?: string
  theme?: ThemeColor
  cta?: ICta
}
